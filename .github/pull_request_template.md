## What this changes


## How I tested it
<!-- Did you actually run the app (npm start) and click through it? -->

## Checklist
- [ ] `npm start` launches and the calculator / player / About dialog work
- [ ] User-visible strings go through the `I18N` dictionary (cs + en)
- [ ] No new runtime dependencies
- [ ] No new inline scripts/styles (CSP stays without `unsafe-inline`)
- [ ] No new unguarded IPC / network / file-system surface
